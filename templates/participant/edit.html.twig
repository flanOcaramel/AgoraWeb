{% extends 'base.html.twig' %}

{% block body %}
<div class="col-sm-8">
    <section class="panel">
        <div class="chat-room-head">
            <h3><i class="fa fa-angle-right"></i> Modifier un participant</h3>
        </div>
        <div class="panel-body">

    {{ include('participant/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_participant_index') }}"><button class="btn btn-primary btn-xs"><i class="fa fa-share"></i></button></a>

    {{ include('participant/_delete_form.html.twig') }}
    </div>
    <!-- fin du div panel-body-->
    </section>
    <!-- fin section participant-->
    </div>
    <!-- fin div col-sm-8-->
{% endblock %}
